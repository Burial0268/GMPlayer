handleLineClick(event) {\n    const line = event.line;\n    // Assuming getLine() returns an object with startTime property\n    const { startTime } = line.getLine();\n    // Emit the event with the correct time value\n    this.$emit('lineClick', startTime);\n}